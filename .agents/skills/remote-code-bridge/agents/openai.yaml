interface:
  display_name: "Remote Code Bridge"
  short_description: "Repo-specific patterns and workflows for remote-code-bridge"
  default_prompt: "Use the remote-code-bridge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true